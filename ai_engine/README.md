# AI Engine

This directory contains YOLO models, detection scripts, preprocessing pipelines, and inference modules.

## Structure
- `/models`: Pre-trained and fine-tuned YOLO weights (`.pt`, `.onnx`, etc.)
- `/scripts`: Detection, tracking, and inference scripts
- `/pipelines`: Data preprocessing and pipeline orchestration
